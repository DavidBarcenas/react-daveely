const initialState = {
  name: 'Dave',
  uid: '132sad1as23d1',
};

export const authReducer = (state = initialState, action: any) => {
  switch (action.type) {
    default:
      return state;
  }
};
